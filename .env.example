# Copy to .env and adjust as needed. Never commit the real .env file.

# Path is relative to backend/ working directory by default.
DATABASE_URL=sqlite:///../data/ledger.db

# Directories (relative to repo root)
IMPORTS_DIR=./imports
BACKUPS_DIR=./backups

LOG_LEVEL=INFO
